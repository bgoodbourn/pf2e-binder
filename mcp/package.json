{
  "name": "pf2e-binder-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "Local MCP server that lets Claude read and write the PF2e binder's scenarios",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=../.env server.js",
    "inspect": "npx @modelcontextprotocol/inspector node --env-file=../.env server.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "@supabase/supabase-js": "^2.117.1",
    "zod": "^4.6.5"
  }
}
